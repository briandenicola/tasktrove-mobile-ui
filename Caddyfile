:80 {
    # Proxy API requests to the TaskTrove backend
    handle /api/* {
        reverse_proxy {$TASKTROVE_BACKEND:localhost:8080}
    }

    # Serve the PWA static files with SPA fallback
    handle {
        root * /srv/tasktrove-ui/dist
        try_files {path} /index.html
        file_server
    }
}
